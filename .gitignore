__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.DS_Store
Thumbs.db
.vscode/
.idea/

# Warehouse and pipeline state — regenerated by `python -m carrier_iq.ingest`
*.duckdb
*.duckdb.wal
.dlt/
!.dlt/config.toml

# dbt
transform/target/
transform/dbt_packages/
transform/logs/

# Never, under any circumstance
.env
.env.*
# ...but the template must be committed: it is what tells the next person
# which variables exist. It carries dummy values only.
!.env.example
*.key
*.pem
data/

# dbt writes a machine-specific anonymous id here. Never committed.
transform/.user.yml
